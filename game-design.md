# Keyboard Mages — Game Design & Architecture Document

> Этот файл предназначен для использования как `CLAUDE.md` / контекстный документ проекта.
> Содержит игровой дизайн, архитектурные решения и открытые вопросы, обсуждённые на этапе планирования.

## 1. Концепция

Браузерная PvP-игра "Keyboard Mages". Игроки сражаются в дуэли 1v1, выбирая заклинания.
Чтобы заклинание применилось, игрок должен быстро и правильно ввести комбинацию символов
(слово-триггер) на клавиатуре. После корректного и своевременного ввода заклинание срабатывает.

## 2. Технологический стек

- **Backend:** NestJS, WebSocket Gateway (Socket.IO), состояние боя — в памяти сервера (source of truth)
- **Frontend:** Angular (UI-оболочка: меню, лобби, HUD, RxJS/Signals для состояния)
- **Боевая сцена / рендер:** PixiJS (спрайты, партиклы) + GSAP (твининг эффектов, тряска экрана и т.п.)
- **Связь:** WebSocket-протокол, сервер валидирует весь игровой ввод

## 3. Архитектура backend (NestJS)

Ключевые модули:
- `MatchmakingModule` — очередь на поиск соперника, создание matchId, пары игроков
- `DuelGateway` — WebSocket-события: `join`, `cast_attempt`, `key_input`, `disconnect`
- `DuelService` — состояние боя в памяти (HP, активные касты, кулдауны, активные эффекты)
- `SpellsModule` — реестр заклинаний (school, tier, type, trigger word, эффекты)
- `EffectsService` — начисление/тик/снятие баффов и дебаффов (см. раздел 6)

### Протокол WebSocket-событий (черновик)

```
server -> both:   match_countdown { remainingMs }         // раз в тик, пока матч ещё не начался
server -> both:   match_started { startedAt }             // один раз, когда истекла пауза после match_found
client -> server: cast_start (spellId)
client -> server: key_input (char, timestamp)   // посимвольно, для live-эффекта у соперника
server -> client: input_ack (correct: bool, progress)
server -> both:   spell_resolved (success, damage, timeTaken)
server -> both:   player_sync { playerId, hp, maxHp, cooldowns: { [spellId]: remainingMs },
                                effects: [{ type, sourceSpellId, remainingMs, magnitude }] }
server -> both:   match_ended { winnerId }                // HP <= 0 ИЛИ дисконнект соперника (раздел 7)
```

**Принципиально:** вся валидация ввода (правильность и скорость) происходит на сервере.
Сервер сам фиксирует momент получения каждого символа (arrival time WebSocket-сообщения),
клиентским timestamp'ам для расчёта результата не доверяем.

## 4. Защита от программного/бот-ввода

- **`KeyboardEvent.isTrusted`** — фильтровать на клиенте события с `isTrusted: false`
  (защищает от JS-инъекций/автоматизации внутри страницы, но не от аппаратных эмуляторов/макросов уровня ОС)
- **Серверные тайминги** — время каста считается по времени получения сообщений сервером, не по клиентским меткам
- **Анализ вариативности интервалов** — слишком равномерные интервалы между нажатиями = признак бота;
  считать дисперсию, флагать аномально низкую вариативность
- **Порог физической скорости** — жёсткий кап (~15-20 нажатий/сек), выше — режект/флаг
- **Накопительная репутация** — не банить за одно подозрительное действие, копить anomaly score за сессию/матчи

## 5. Система заклинаний

### 5.1 Три оси классификации
Каждое заклинание характеризуется: **название**, **описание**, **школа**, **тип**, **уровень сложности**.

### 5.2 Уровни сложности

| Уровень | Длина триггера | Кулдаун | Окно на ввод | Мощь |
|---|---|---|---|---|
| 1 (Базовый) | 4–6 символов | 3–6 сек | ~1.5–2 сек | Низкая, доступна часто |
| 2 (Продвинутый) | 7–10 символов | 10–15 сек | ~2.5–3.5 сек | Средняя, тактическая |
| 3 (Ультимативный) | 11–16 символов | 25–40 сек | ~4–6 сек | Высокая, решающая |

Окно на ввод рекомендуется завязывать на целевой WPM, а не на фиксированное время,
чтобы не дискриминировать медленно печатающих игроков.

Урон Attack-заклинаний в таблицу не входит: он не выводится из тира, а задаётся
диапазоном `[min, max]` индивидуально на каждом заклинании (`Spell.damage`, раздел 7.6) —
конкретные числа см. в `backend/src/spells/spell-roster.ts`.

### 5.3 Типы

- **Атака (Attack)** — прямой урон, часто с доп. эффектом
- **Защита (Defense)** — щиты, снижение урона, отражение
- **Поддержка (Support)** — хил, баффы, дебаффы, контроль, изменение кулдаунов

### 5.4 Школы и ростер заклинаний

#### 🔥 Огонь (Fire)
| Название | Триггер | Тип | Ур. | Описание |
|---|---|---|---|---|
| Spark | `spark` | Атака | 1 | Небольшая искра, малый прямой урон |
| Ember Ward | `guard` | Защита | 1 | Огненная плёнка, снижает входящий урон |
| Blaze | `inferno` | Атака | 2 | Вспышка пламени + поджог (DoT 3 сек) |
| Phoenix Rebirth | `phoenixrise` | Поддержка | 3 | Исцеление + временный щит от смертельного удара |
| Inferno Storm | `firestormrage` | Атака | 3 | Мощный взрыв, максимальный прямой урон школы |

#### ❄️ Лёд (Ice)
| Название | Триггер | Тип | Ур. | Описание |
|---|---|---|---|---|
| Frostbite | `frost` | Атака | 1 | Малый урон + лёгкое замедление ввода соперника |
| Winter's Grasp | `chill` | Поддержка | 1 | Замедляет восстановление кулдауна соперника |
| Ice Shard | `iceshard` | Атака | 2 | Средний урон + краткое замедление ввода соперника |
| Frozen Armor | `glacierwall` | Защита | 2 | Ледяной щит: поглощение + лёгкое отражение |
| Blizzard | `absolutezero` | Атака | 3 | Крупный урон + сильное замедление |

#### ✨ Тайная магия (Arcane)
| Название | Триггер | Тип | Ур. | Описание |
|---|---|---|---|---|
| Arcane Blast | `blast` | Атака | 1 | Стабильный урон без побочных эффектов |
| Power Rune | `power` | Поддержка | 1 | Усиливает исходящий урон заклинателя |
| Arcane Missile | `starfall` | Атака | 2 | Урон, игнорирующий эффекты снижения урона |
| Mirror Shield | `reflectio` | Защита | 2 | Отражает часть входящего урона |
| Time Warp | `chronoshift` | Поддержка | 3 | Сбрасывает кулдаун заклинаний 2 и 3 тира своей школы |
| Spellbreaker | `spellbreaker` | Атака | 3 | Крупный урон + усиление исходящего урона заклинателя |

#### 🌀 Хаос (Chaos)
| Название | Триггер | Тип | Ур. | Описание |
|---|---|---|---|---|
| Chaos Bolt | `chaos` | Атака | 1 | Урон варьируется в широком диапазоне |
| Unstable Surge | `wildmagic` | Поддержка | 2 | Случайный эффект заклинателю: сопротивление, урон себе, или изменение урона в бою |
| Entropy Veil | `entropyveil` | Защита | 2 | Случайно: полный блок с отражением заклинания ИЛИ рост входящего урона на 50% |
| Corruption | `corruption` | Атака | 2 | Урон + DoT на соперника со случайной длительностью |
| Void Collapse | `voidcollapse` | Атака | 3 | Огромный урон, но увеличивает время восстановления следующего заклинания |

#### 🌿 Природа (Nature)
| Название | Триггер | Тип | Ур. | Описание |
|---|---|---|---|---|
| Vine Whip | `vine` | Атака | 1 | Малый урон + блокирует случайное заклинание соперника на 1 секунду |
| Bark Skin | `barkskin` | Защита | 1 | Пассивное снижение урона на время действия |
| Entangle | `rootbind` | Поддержка | 2 | Блокирует случайное заклинание соперника на 3 секунды |
| Regrowth | `lifebloom` | Поддержка | 2 | Восстанавливает HP заклинателю по 3 ед. каждую секунду в течение 2 секунд |
| Nature's Fury | `wildgrowthrage` | Атака | 3 | Крупный урон + блокирует случайное заклинание соперника на 5 секунд |
| Vampiric Roots | `vampiricroots` | Атака | 3 | Крупный урон + восстанавливает HP заклинателю по 3 ед. каждую секунду в течение 4 секунд |

## 6. Система эффектов (баффы/дебаффы)

### 6.1 Модель данных

```typescript
interface ActiveEffect {
  id: string;                 // uuid инстанса каста
  effectType: EffectType;     // 'damage_boost' | 'slow' | 'dot' | 'hot' | 'shield' | 'silence' | 'cooldown_reduction'
  sourceSpellId: string;
  targetPlayerId: string;
  magnitude: number;          // сила эффекта (%, флэт, множитель)
  durationMs: number;
  appliedAt: number;          // серверный timestamp
  tickIntervalMs?: number;    // для DoT/HoT
}
```

### 6.2 Правило стакинга (зафиксировано)

- Длительность дебаффа/баффа не превышает кулдаун заклинания, которое его накладывает
  → самоперекрытие одного и того же заклинания невозможно физически
- Эффекты **разных** заклинаний (в т.ч. с одинаковым `effectType`) тикают **параллельно**,
  идентифицируются по `sourceSpellId`/`instanceId`, а не по типу эффекта
- Стакинг всегда `stack` — отдельная логика `refresh`/`ignore` не нужна
- Единственное условие удаления эффекта — истечение `durationMs`

### 6.3 Комбинирование модификаторов — МУЛЬТИПЛИКАТИВНО

Зафиксировано решение: при наложении нескольких однотипных эффектов (например, несколько
источников замедления) они комбинируются мультипликативно, а не аддитивно — чтобы избежать
"лока" игрока при 3-4 одновременных дебаффах (естественное затухание отдачи).

```typescript
function getEffectiveCastWindow(base: number, effects: ActiveEffect[]): number {
  const slowEffects = effects.filter(e => e.effectType === 'slow');
  const multiplier = slowEffects.reduce((acc, e) => acc * (1 + e.magnitude), 1);
  return base * multiplier;
}
```

Аналогично применяется для `damage_boost`, `cooldown_reduction` и т.д. — всегда произведение
модификаторов, не сумма.

### 6.4 Тикающий цикл боя

Общий heartbeat на сервере (например, `setInterval` раз в 250-500мс на матч):
- применяет DoT/HoT по `tickIntervalMs`
- проверяет истечение `durationMs`, удаляет истёкшие эффекты
- пересчитывает эффективные модификаторы (урон, cast window, доступность каста)
- рассылает `player_sync` клиентам (переименован из `effects_sync` — раздел 7.30, теперь несёт
  не только эффекты, но и `hp`/`maxHp`/`cooldowns`)

Клиент только отображает присланное состояние (иконки баффов с таймером), не считает
логику истечения самостоятельно. Иконка эффекта берётся по `sourceSpellId` (см. раздел 7.25) —
одна и та же картинка используется и в панели кастов, и над персонажем как индикатор баффа/дебаффа.

## 7. Решённые вопросы дизайна

1. **Контрольные эффекты (рут/заморозка/тишина)** — отдельного `Spell.type` не вводим.
   Контроль остаётся тегом на уровне `ActiveEffect.effectType` (`slow`/`silence`/будущий `root`
   и т.д.), а `Spell.type` (Attack/Defense/Support) описывает роль заклинания в бою, а не наличие
   контроля как побочного эффекта. Уже соответствует реализации в `backend/src/effects/effect.types.ts`.
2. **Слова-триггеры** — остаются тематическими английскими словами (ростер раздела 5.4).
   Приоритет отдан фэнтези-погружению над абсолютной языковой нейтральностью замера скорости
   печати; это осознанный компромисс, типичный для жанра typing-race.
3. **Штраф за опечатку** — полный сброс прогресса каста (реализовано в
   `DuelService.handleKeyInput`, `backend/src/duel/duel.service.ts`). Осознанный риск: одна
   опечатка на 15-м символе 16-символьного ultimate-триггера обнуляет весь каст — держим в уме
   как кандидата на пересмотр после первых игровых тестов баланса заклинаний уровня 3.
4. **Синергии школ / комбо-заклинания** — отложены за пределы MVP (см. раздел 8). Базовый
   боевой цикл (25 заклинаний + система эффектов) должен быть играбелен и протестирован до того,
   как добавлять систему комбо между двумя игроками.
5. **Пауза перед стартом боя** — матч начинается не сразу по `match_found`, а спустя
   3 секунды (`DuelService.PRE_MATCH_DELAY_MS`, `MatchState.startsAt`). До этого момента
   `DuelGateway` рассылает `match_countdown { remainingMs }` вместо боевого тика, а
   `DuelService.startCast` отклоняет любые касты с `reason: 'match_not_started'`. По истечении
   паузы один раз уходит `match_started`, и обычный heartbeat (эффекты/касты) включается.
6. **Урон Attack-заклинаний — диапазон, не значение по тиру.** Каждое Attack-заклинание
   задаёт собственный `damage: { min, max }` (`backend/src/spells/spell.types.ts`), фактический
   урон бросается случайно в этом диапазоне при разрешении каста (`DuelService.rollDamage`).
   Заклинания одного тира могут иметь разную мощь и разброс (например, Spark 6–8 против общего
   плейсхолдера Basic-урона). Заклинания, для которых индивидуальный диапазон ещё не откалиброван,
   временно используют старое фиксированное значение по тиру как flat-диапазон `[v, v]`
   (`PLACEHOLDER_DAMAGE_BY_TIER` в `spell-roster.ts`) — предмет дальнейшей балансировки.
   Дополнительно заклинание может объявить `Spell.onHitEffect` — эффект, накладываемый при
   успешном попадании независимо от типа заклинания, по умолчанию на соперника кастера
   (например, Blaze: поджог `dot` 2 урона/тик на 3 сек вместе с уроном; Winter's Grasp:
   `cooldown_reduction` с положительным `magnitude` — Support-заклинание без урона, которое
   замедляет восстановление кулдауна соперника), либо на самого кастера через `target: 'self'`
   (Frozen Armor, см. п.8). Знак `magnitude` для мультипликативных типов эффекта (`slow`,
   `damage_boost`, `cooldown_reduction`) значим: положительный усиливает/удлиняет базовую
   величину, отрицательный ослабляет/сокращает (см. `EffectsService.combinedMultiplier`,
   формула `base * (1 + magnitude)`). `EffectType: 'silence'` (блокировка конкретного
   `spellId` — не путать с заклинанием Power Rune, бывш. Silence, раздел 7.14, которое давно
   переделано в самобаф урона и с этим типом эффекта не связано) и хил — свои эффекты
   Support-заклинаний, которые generic `onHitEffect` пока не покрывает и которые остаются
   открытым вопросом (см. TODO в `DuelService.buildResolution`).
7. **Эффекты, зависящие от навыка игрока, — избегаем.** Изначальная идея Ice Shard
   ("шанс скрыть подсказку ввода на 1 сек") отклонена: скрытие подсказки не мешает опытному
   игроку, печатающему по памяти, и наказывает только новичков — обратный от желаемого эффект
   баланса. Заменено на гарантированный (не по шансу) `slow`, как у Frostbite — работает
   одинаково независимо от того, смотрит ли игрок на подсказку. Держим это как общий принцип
   при добавлении будущих эффектов: приоритет отдаём тем, что бьют по объективному игровому
   состоянию (время, HP, кулдауны), а не по UI-подсказкам, которые опытные игроки не используют.
8. **Щит (Frozen Armor) — отдельный конвейер урона, не сводится к multiplier-эффектам.**
   `effectType: 'shield'` использует `magnitude` как флэт-пул HP (не множитель): при входящем
   Attack-уроне (`DuelService.buildResolution`) сначала вызывается
   `EffectsService.absorbWithShields`, которая списывает урон с активных щитов цели по очереди
   создания, пока урон не закончится или щиты не исчерпаются; остаток идёт по HP как обычно.
   "Лёгкое отражение" — доля поглощённого (не входящего) урона, `ActiveEffect.reflectRatio`
   (0.2 у Frozen Armor), возвращается атакующему как урон по его HP. Щит вешается на самого
   кастера (`onHitEffect.target: 'self'`) и живёт `durationMs`, даже если не исчерпан за это
   время — как и другие эффекты, снимается общим `pruneExpired`.
9. **Ember Ward — процентное снижение входящего урона, без нового `effectType`.**
   Вместо отдельного "damage_reduction" переиспользуем `'damage_boost'`: `magnitude`
   по-прежнему множитель `(1 + magnitude)` для урона, но у Ember Ward он навешивается на
   самого кастера (`onHitEffect.target: 'self'`) с отрицательным значением (`-0.25` → урон
   ×0.75 на 1.5 сек). `DuelService.buildResolution` теперь считает `getEffectiveDamage` сразу
   по объединению эффектов атакующего И цели — баффы атакующего (положительный `magnitude`)
   и защита цели (отрицательный) комбинируются в одном мультипликативном проходе. В отличие
   от Frozen Armor это не отдельный пул HP: пока эффект активен, он ослабляет весь входящий
   урон, а не тратится за один удар. `durationMs` (1500) намеренно короче `cooldownMs`
   Basic-тира (2000) — иначе перекаст держал бы 25%-снижение урона 100% времени боя.
10. **Кулдаун общий на весь тир внутри школы, а не на конкретное заклинание.** Успешный каст
    любого заклинания ставит на кулдаун ВСЕ заклинания той же школы и того же тира
    (`SpellsService.getTierMates`, `DuelService.buildResolution`) — например, Inferno Storm
    (Fire, Ultimate) кладёт на кулдаун и себя, и Phoenix Rebirth (Fire, Ultimate), но не
    трогает Ultimate-заклинания других школ. Явно НЕ глобально по всем школам: игрок будет
    выбирать школу мага перед поиском матча и сможет кастовать только заклинания этой школы
    (см. открытый вопрос ниже) — на практике "весь тир школы" и есть "все доступные игроку
    заклинания этого тира". Сейчас `DuelService.startCast` ещё не ограничивает `spellId` по
    выбранной школе — это отдельная задача, без неё игрок технически может запросить любой
    `spellId` из ростера независимо от выбранного мага. *(Обновление после раздела 7.12: выбор
    школы теперь ХРАНИТСЯ на сервере как `PlayerState.school`, но `startCast` его всё ещё не
    проверяет — см. открытый вопрос ниже.)*
11. **Phoenix Rebirth — мгновенный хил + одноразовое "не даёт умереть" (`death_ward`).**
    `Spell.healOnCast` (10) — прямое восстановление HP кастера в момент разрешения каста,
    отдельно от `onHitEffect`: не тикающий эффект, а мгновенное изменение, как урон у Attack.
    `onHitEffect: { type: 'death_ward', target: 'self', magnitude: 1, durationMs: 4000 }` —
    пока активен, следующий смертельный удар не убивает кастера, а оставляет его с `magnitude`
    HP и расходует эффект (одноразовый). Реализовано через единый `DuelService.applyDamage()`,
    через который теперь проходит **весь** урон по HP (прямой удар, отражение щитом, DoT-тик) —
    это нужно, чтобы death_ward одинаково спасал от любого источника урона, а не только от
    прямого попадания Attack-заклинания. `durationMs` (4000) короче `cooldownMs` Ultimate-тира
    (6000) по той же причине, что и у Ember Ward — не давать перманентный аптайм эффекта.
12. **`PlayerState.school` — школа мага хранится на сервере (осталась после ревизии п.14).**
    Изначально понадобилась для старой версии Power Rune/Spellbreaker ("случайное заклинание
    школы соперника" — см. историю ревизий п.14), которая позже была пересмотрена и от этой
    зависимости отказалась. Поле и вся прокладка НЕ убраны, а намеренно оставлены: это
    единственная реализованная часть открытого вопроса раздела 8 п.4 (сервер должен знать
    школу игрока, чтобы позже ограничить, какие `spellId` он может кастовать) — было бы
    странно выкидывать её только для того, чтобы вскоре добавить заново. Прокинуто через весь
    matchmaking-путь: `FindMatchPayload.school` → `MatchmakingService.enqueue(playerId,
    school)` → `DuelService.createMatch(playerAId, playerASchool, playerBId, playerBSchool)` →
    `PlayerState.school`. Фронтенд пока этот payload не отправляет. `startCast` пока НЕ
    проверяет `spell.school === player.school` — само ограничение всё ещё не реализовано
    (раздел 8, п.4).
13. **Arcane Missile игнорирует ЧУЖУЮ защиту, но не щиты.** `Spell.ignoresDamageReduction`
    убирает эффекты ЦЕЛИ (`this.effectsOn(match, targetId)`) из расчёта `getEffectiveDamage` —
    то есть обходит именно вещи вроде Ember Ward (`damage_boost` с отрицательным `magnitude`
    на самой цели). Щиты (`shield`, Frozen Armor) — отдельный конвейер (`absorbWithShields`,
    п.8) и им не затрагиваются: Arcane Missile всё ещё можно заблокировать щитом, только не
    процентным снижением урона. Осознанное сужение "части защиты" из исходного описания.
14. **Power Rune / Spellbreaker — ревизия основной механики: баф исходящего урона себе, а НЕ
    кулдаун случайному заклинанию соперника.** Первая версия (`Spell.disableRandomSpell:
    { durationMs }`, `DuelService.disableRandomSchoolSpell`, `SpellsService.getBySchool`) при
    успехе кастера ставила на кулдаун случайное заклинание ИЗ ШКОЛЫ ЦЕЛИ. Полностью заменена
    (весь код и типы удалены, не оставлено закомментированным) на самобаф через обычный
    `onHitEffect`: `{ type: 'damage_boost', target: 'self', damageDirection: 'outgoing' }` —
    Power Rune (бывш. Silence, `magnitude: 0.15`, 2 сек), Spellbreaker (`magnitude: 0.3`, 4 сек, вдобавок к
    урону 24–28). Использует уже существующий конвейер `damage_boost`/`damageDirection`
    (п.17) — новой инфраструктуры не потребовалось. `PlayerState.school` (п.12) для этой
    механики больше не нужен, но оставлен ради открытого вопроса раздела 8 п.4.
15. **`reflect` — новый `EffectType` для Mirror Shield, отдельно от `shield.reflectRatio`.**
    В отличие от щита Frozen Armor (одноразовый пул HP, реф ставится через
    `ActiveEffect.reflectRatio` и отражает долю от ПОГЛОЩЁННОГО щитом урона), `reflect` — это
    длящийся бафф без пула: пока активен (`durationMs`, 2.5 сек у Mirror Shield), КАЖДЫЙ
    полученный удар отражает `magnitude`-долю (0.2) обратно атакующему, не расходуясь и не
    снижая урон, который получает сам защищающийся. Считается уже после поглощения щитом, если
    оно тоже есть, чтобы не задваивать эффект от одного и того же удара.
16. **Time Warp — `resetsCooldownTiers`, включая собственный тир, с ожидаемым нюансом.**
    `Spell.resetsCooldownTiers: [Advanced, Ultimate]` обнуляет `cooldowns` всех заклинаний этих
    тиров своей школы ДО стандартного блока кулдауна (п.10). Так как Time Warp сам Ultimate,
    сброс тира 3 в рамках этого же каста визуально не заметен: стандартный блок сразу следом
    безусловно ставит кулдаун на весь Ultimate-тир заново (иначе получился бы бесплатный
    Time Warp без кулдауна вообще). Реальный эффект — только тир 2 (Advanced) освобождается.
    Оставлено как есть, а не убрано: намеренно соответствует дословной формулировке задачи,
    и не ломает баланс, раз тир 3 всё равно перезаписывается.
17. **`damage_boost` получил направление (`damageDirection: 'outgoing' | 'incoming'`) — задним
    числом исправлен скрытый баг Ember Ward.** До этого пункта единый механизм `damage_boost`
    комбинировал эффекты атакующего И цели в одном мультипликативном проходе без различия
    "чей это баф и на что он влияет" — из-за этого Ember Ward (магнитуда −0.25 на самом
    кастере) фактически ослаблял и ЕГО СОБСТВЕННЫЙ исходящий урон, пока щит активен, а не
    только входящий, как задумано. Понадобилось выявить и исправить при реализации Unstable
    Surge (раздел 5.4), где ровно этот же тип эффекта должен давать ДВА явно разных случайных
    исхода — "усиление исходящего" и "усиление входящего" — и без направления они были бы
    неотличимы. `DuelService.effectsForDamageRole(match, playerId, role)` фильтрует
    `damage_boost`-эффекты игрока по `damageDirection` перед передачей в `getEffectiveDamage`;
    отсутствие `damageDirection` трактуется как "действует в обе стороны" (обратная
    совместимость). Ember Ward обновлён (`damageDirection: 'incoming'`).
18. **`Spell.randomEffectPool` — как `onHitEffect`, но выбирается один случайный элемент.**
    Introduced для Unstable Surge и Entropy Veil: при успешном касте `DuelService`
    выбирает ровно один элемент массива (`Math.random()`) и применяет его точно так же, как
    единственный `onHitEffect` — обобщено в одну переменную `chosenEffect` в
    `buildResolution`, обе схемы (фиксированный эффект и случайный пул) взаимоисключающи и
    делят один и тот же код применения.
19. **Unstable Surge — все 4 случайных исхода бьют по САМОМУ ЗАКЛИНАТЕЛЮ, включая два
    негативных.** Сопротивление (−30%, 2с) и усиление исходящего урона (+20%, 3с) — баффы;
    периодический урон себе (3/тик, 3 тика по 1с) и усиление ВХОДЯЩЕГО урона себе (+20%, 3с) —
    даунсайды. Осознанное расширение темы "нестабильная хаотичная магия может навредить и
    самому магу" (см. также Entropy Veil, п.20) — заклинание с шансом самому себе навредить,
    а не только рандомный бафф.
20. **Entropy Veil — "отражает заклинание" реализовано через shield с большим пулом
    (999) + `reflectRatio: 1`, а не отдельным одноразовым механизмом.** Технически это
    "поглощает и отражает всё, пока не потратит пул или не истекут 2 секунды", а не строго
    гарантированное "ровно одно заклинание" — если два вражеских удара попадут в это окно,
    оба будут поглощены и отражены. Выбрано намеренно как приближение без новой инфраструктуры
    (использует уже существующий `absorbWithShields` + `reflectRatio` из Frozen Armor/раздела
    7.8), при обычном темпе боя (кулдауны/окно каста) второй удар в те же 2 секунды
    маловероятен, но не невозможен.
21. **Void Collapse — `cooldown_delay`, новый `EffectType` для ФЛЭТ-добавки к кулдауну
    (не процент).** В отличие от `cooldown_reduction` (мультипликативный, `base * (1 +
    magnitude)`, см. Winter's Grasp/п.6), `cooldown_delay` — это прямая добавка в мс
    (`magnitude: 3000`) к `effectiveCooldown` следующего успешного каста ТОГО ЖЕ игрока.
    Процент не подошёл бы: одна и та же "штраф 3 секунды" даёт разный относительный эффект
    для разных тиров (150% для Basic-кулдауна 2000мс против 50% для Ultimate-кулдауна 6000мс).
    Реализовано как одноразовый расходуемый эффект (по аналогии с `death_ward`, раздел 7.11):
    `DuelService` считывает и СРАЗУ удаляет `cooldown_delay`-эффекты кастера в момент, когда
    ставит кулдаун за следующий успешный каст — гарантирует именно "следующее заклинание",
    а не "любое в течение X секунд". `durationMs` (15000) — просто защитный потолок на случай,
    если игрок долго не кастует ничего вообще.
22. **Механизм "кулдаун случайному заклинанию школы соперника" — возвращён, но уже для Nature,
    не Arcane.** В п.14 этот же механизм (`Spell.disableRandomSpell`,
    `DuelService.disableRandomSchoolSpell`, `SpellsService.getBySchool`) был полностью удалён
    после ревизии Power Rune/Spellbreaker. Ничего не пришлось изобретать заново концептуально —
    код восстановлен в точности как был (тот же файл истории git это покажет), только
    подключён к другим заклинаниям: Vine Whip (1 сек), Entangle (3 сек), Nature's Fury (5 сек).
    Значение `PlayerState.school` (п.12), которое было оставлено "про запас" именно на такой
    случай, теперь снова используется по прямому назначению.
23. **Bark Skin — дословная копия эффекта Ember Ward.** По прямому запросу: тот же
    `onHitEffect` (`damage_boost`, `target: 'self'`, `magnitude: -0.25`, `durationMs: 1500`,
    `damageDirection: 'incoming'`), что и у `fire_ember_ward`. Два разных заклинания разных школ
    с идентичной механикой — осознанно, не считается дублированием кода (баланс/тематика важнее
    DRY на уровне игровых данных).
24. **Vampiric Roots — новое Ultimate-заклинание Nature, добавленное по запросу без
    уточнённого названия.** Требование было "такой же диапазон урона, что и у Nature's Fury
    (28–32), но с HoT на заклинателя, исцеляющим на 3 ед. в течение 4 секунд" — имя и триггер
    (`vampiricroots`, 13 символов, укладывается в диапазон Ultimate-триггеров 11–16) придуманы
    по тематике "заклинание-вампир": урон противнику + жизнь себе. Тиковый интервал HoT (1 сек,
    итого 4 тика по 3 = 12 HP) принят по аналогии с Regrowth, которая явно описана как "3 ед.
    КАЖДУЮ секунду" — формулировка для Vampiric Roots не уточняла тик явно, интервал 1 сек
    выбран как разумное умолчание, а не единственно возможное прочтение.
25. **Иконки эффектов — по одной на `spellId`, переиспользуются и в панели кастов, и как
    индикатор баффа/дебаффа над персонажем.** Явное решение (снимает открытый вопрос раздела 8,
    бывший п.1): НЕ единая иконка на `EffectType` с общим счётчиком — у каждого заклинания своя
    картинка, и она же показывается над персонажем, когда его эффект активен. Прямое следствие:
    если и Blaze, и Inferno Storm держат на цели `dot` одновременно, это два раздельных
    `ActiveEffect` с разными `sourceSpellId` (бэкенд и так их не мёржил — `buildResolution`
    всегда пушит новый объект с собственным `id`, не ищет существующий эффект того же типа для
    продления) — и на клиенте это будут две раздельные иконки (Blaze / Inferno Storm), а не одна
    с "×2". Единственное, чего не хватало технически: событие (тогда `effects_sync`, позже
    переименовано в `player_sync` — раздел 7.30) слало `{ type, remainingMs, magnitude }` без
    `sourceSpellId`, так что клиент не мог понять, ЧЬЁ это горение — добавлено поле
    `sourceSpellId` в payload (`DuelGateway`, раздел 3). Сами файлы иконок и вёрстка HUD —
    отдельная задача, ждёт ассетов (раздел 8, п.2).
26. **Первая реальная (не TEMP) боевая механика на фронте: панель заклинаний + окно ввода
    триггера, по-настоящему подключённые к `cast_start`/`key_input`.** `App.youSpells` берёт
    кнопки из нового `frontend/src/app/spells-data.ts` — облегчённой проекции ростера (только
    `id`/`name`/`trigger`, без игровой логики) для трёх школ, у которых уже готовы иконки
    (fire/ice/arcane; nature/chaos ждут ассетов). Клавиши `1..N` (обработчик `App.onKeydown`)
    выбирают заклинание по индексу в этом списке — диапазон подстраивается под реальное число
    заклинаний школы (у Arcane их 6, не 5). После выбора те же нажатия клавиш идут в
    `SocketService.sendKey` как `key_input`, пока открыто окно (`activeCast`), и не
    интерпретируются как новый выбор заклинания. `SocketService` сама восстанавливает
    `deadline` из `cast_started` и локально закрывает окно по таймауту как подстраховку — сервер
    молча чистит просроченный каст в `DuelService.tick()`, не оповещая клиента (см. раздел 6.4),
    так что без этой подстраховки окно ввода зависало бы, если игрок бросает печатать. Анимация
    атаки (`triggerAttack`) теперь также запускается автоматически на любой успешный
    `spell_resolved` (`SocketService.lastResolved`, слушает исходы ОБОИХ игроков, не только
    своего) — единственная доступная анимация каста сейчас, используется для любого типа
    заклинания (Attack/Defense/Support), не только атакующих. Заодно поправлено
    несоответствие: `MageType` на фронте использовал `'frost'` вместо `'ice'` и не знал про
    `'arcane'` — приведено в соответствие с `SpellSchool` бэкенда (значения теперь совпадают
    дословно), `onFindMatch` начал реально слать выбранную школу в `find_match` вместо ничего.
    Заодно подключён спрайт arcane-mage (те же 8f/10f-сетки и кейфреймы, что у frost/chaos —
    ассеты игрок уже добавил, просто раньше не были подключены к CSS).
27. **Спеллбук на главном экране — постоянно открытое окно выбора школы + просмотра заклинаний,
    заменяет прежний маленький ряд иконок школ.** По явному запросу: окно без хэдера, не
    закрывается, всегда на экране до статуса `'found'` (`@if (socket.status() !== 'found')`,
    как и раньше у footer). Слева — те же кнопки школ (`SCHOOL_OPTIONS`,
    `frontend/src/app/schools-data.ts`; иконка для льда называется `frost.png`, а не `ice.png` —
    маппинг `id → filename` не всегда 1:1) и список заклинаний выбранной школы; клик по
    заклинанию НЕ кастует (это не бой, матча ещё нет) — просто показывает
    название/описание/урон/эффект/кулдаун (`App.selectedSpell`, поля добавлены в
    `SpellButtonData` в `spells-data.ts`: `description`, `damage`, `effect`, `cooldownSec` —
    человекочитаемые строки, отдельно от игровых данных на бэкенде, вручную синхронизированы
    с `spell-roster.ts` на момент написания). Справа — крупный (`clamp(14rem,30vw,22rem)`)
    idle-спрайт выбранной школы, переиспользует те же классы/кейфреймы `.character-sprite--*`,
    что и боевая сцена, без модификатора `--attack` (просто анимация покоя). Выбор школы здесь
    ЕСТЬ выбор для `find_match` — `App.selectSchool` пишет прямо в `youMageType`, тот же сигнал,
    что раньше двигали TEMP debug-кнопки "Вы: …" (эти кнопки убраны как избыточные; кнопки
    "Соперник: …" остались — превью спрайта оппонента по-прежнему не связано с реальным матчем).
    Смена школы сбрасывает выбранное для просмотра заклинание (`selectedSpellId.set(null)`).
    Заодно переименована папка ассетов `public/druid-wizard` → `public/nature-wizard` (была не
    подключена нигде в коде, поэтому чисто переименование без последствий) и подключён её спрайт
    (`character-sprite--nature`) — та же 8f/10f-сетка, что у остальных магов.
28. **Спеллбук — без скролла, плюс данные Chaos/Nature.** `.spellbook-list` лишился
    `max-height`/`overflow-y` (список растёт по контенту), `.spellbook` получил `min-height: 30rem`
    вместо `20rem` — рассчитано на самую длинную школу (Arcane и Nature — по 6 заклинаний).
    `SPELL_BUTTONS_BY_SCHOOL` в `spells-data.ts` пополнился записями для Chaos (5) и Nature (6) —
    те же поля `description`/`damage`/`effect`/`cooldownSec`, вручную перенесённые из
    `spell-roster.ts` (эффекты со случайным исходом вроде Unstable Surge/Entropy Veil описаны
    одной строкой, перечисляющей все варианты). Иконки для Chaos/Nature добавлены позже
    (см. п.29) — на момент этого пункта их ещё не было.
29. **Иконки Chaos/Nature добавлены; обводка кнопки каста по тиру (синяя/фиолетовая/золотая).**
    `public/spell-icons/` пополнился файлами для всех 11 заклинаний Chaos+Nature — те же две
    заглавные `.PNG`, что и раньше у Arcane (раздел 6/п.12 истории), переименованы в `.png` тем же
    приёмом (`git mv` через временное имя, т.к. Windows FS регистронезависима и `git mv` напрямую
    A→A с другим регистром не сработал бы). `SpellButtonData` получил поле `tier: 1 | 2 | 3`
    (совпадает с backend `SpellTier`, заполнено вручную по каждому заклинанию) — используется
    только для цвета обводки `.spell-btn` в бою (`.spellbook-item` в спеллбуке не тронут, там
    обводки по тиру не просили). Новые CSS-переменные `--tier-basic` (синий), `--tier-epic`
    (фиолетовый — не переиспользует `--arcane`, хотя цвет близкий: разные по смыслу свойства,
    не хотим, чтобы правка бренд-цвета школы Arcane случайно поменяла цвет "эпических"
    заклинаний всех школ) и `--tier-legendary` (золотой). Классы `.spell-btn--tier-{1,2,3}`
    объявлены сразу после базового `.spell-btn` — `:hover`/`--active`/`:disabled` идут ниже по
    файлу и перекрывают цвет обводки при равной специфичности (та же логика каскада, что и раньше
    в этом разделе). Проверено в браузере: Chaos Bolt (тир 1) — синяя, Unstable Surge/Entropy
    Veil/Corruption (тир 2) — фиолетовая, Void Collapse (тир 3) — золотая.
30. **Реальный HP-бар, летающие циферки урона/хила, блюр+таймер кулдауна на кнопках заклинаний.**
    Все три фичи используют ОДИН расширенный тиковый sync, переименованный `effects_sync` →
    `player_sync` (раздел 3/6.4) — раз он и так уходит раз в тик (~300мс) на каждого игрока,
    логично довесить туда `hp`/`maxHp` (экспортирован `MAX_HP` из `duel.service.ts`) и
    `cooldowns: { [spellId]: remainingMs }` (`DuelGateway` берёт готовый
    `PlayerState.cooldowns`, фильтрует только записи с `readyAt > now` — иначе клиент получал бы
    вечно растущую карту с давно готовыми заклинаниями). HP-бар: `.hp-fill` получил
    `[style.width.%]` от `hp/maxHp*100`, вместо всегда-100%-ширины.
    Летающие циферки: НЕ отдельное событие на "урон случился" — фронтенд (`SocketService`)
    сравнивает новое `hp` с предыдущим значением при каждом `player_sync` и, если оно изменилось,
    публикует разовое `lastHpChange` (delta, с уникальным id, чтобы сигнал менялся даже при
    одинаковой дельте два раза подряд). Это осознанно универсальный механизм: он одинаково
    ловит прямой удар Attack-заклинания, DoT-тик, отражение щитом и хил — все источники
    изменения HP, включая те (DoT, reflect), для которых отдельного события в принципе нет
    (раздел 6.4/7.11). Компонент (`App`) слушает `lastHpChange` через `effect()`, добавляет
    циферку в массив `youFloatingNumbers`/`foeFloatingNumbers` и удаляет её через
    `FLOATING_NUMBER_LIFETIME_MS` (1000мс, должно совпадать с длительностью CSS-анимации
    `float-up`) — по тому же паттерну, что уже применялся для анимации атаки. Кулдаун на кнопке:
    `.spell-btn-cooldown` — `position:absolute; inset:0; backdrop-filter: blur(2px)` поверх
    иконки, с текстом таймера (`formatCooldown`, секунды с одним знаком после запятой) поверх
    самого блюра (сам оверлей не блюрится, блюрится то, что ПОД ним — иконка). Кнопка получает
    `disabled`, когда `youCooldowns()[spellId]` определён — `castSpellByIndex`/`onKeydown`
    дополнительно проверяют то же самое клиентски, не дожидаясь отказа от сервера. Проверено в
    браузере через прямой JS-вызов `castSpell`/`sendKey` (реальные повторные касты через
    ручной `computer`-клик слишком медленные относительно 2-секундного окна Basic-тира —
    см. ту же проблему таймингов, что и в разделе 7.26): HP соперника корректно уменьшался,
    циферка `-21` появлялась над персонажем, кнопки 1-2 (общий тир-кулдаун) блокировались с
    видимым блюром и таймером `1.3`/`5.9`.
31. **Экран результата матча (`match_ended` → "ПОБЕДА"/"ПОРАЖЕНИЕ" + "Вернуться на главную").**
    `match_ended { winnerId }` уже рассылался бэкендом (`DuelGateway`, при `finishedAt`), но
    фронтенд его вообще не слушал — просто не было ни одного `.on('match_ended', ...)` в
    `SocketService`. Добавлен: `matchResult = signal<'win'|'loss'|null>`, вычисляется
    сравнением `winnerId === playerId`. Оверлей (`.match-result-backdrop`) — отдельный
    top-level `@if`, НЕ вложенный в `@if (socket.status() !== 'found')`: матч в момент
    `match_ended` всё ещё в статусе `'found'` (никто его не менял), так что если бы блок был
    вложен, экран результата никогда бы не показался — он должен появляться ПОВЕРХ всё ещё
    отрисованной (замороженной) боевой сцены, а не вместо нее. Кнопка "Вернуться на главную"
    (`App.returnToMenu`) вызывает новый `SocketService.resetMatch()` — сбрасывает ВСЁ
    состояние конкретного матча (`status`→`'idle'`, `match`, `activeCast`, `hpByPlayerId`,
    `cooldownsByPlayerId`, `matchResult` и т.д.), но НЕ трогает сам сокет (он ещё нужен для
    следующего `find_match`) — плюс компонент сам сбрасывает свои локальные визуальные остатки
    (анимации атаки, летающие циферки), которые вообще не хранятся в `SocketService`. Цвета:
    "ПОБЕДА" — `--tier-legendary` (золотой, тот же что у Ultimate-обводки), "ПОРАЖЕНИЕ» —
    `--hp` (красный, тот же что у HP-бара) — переиспользование уже существующих переменных,
    без новых цветов. Проверено в браузере на реальном матче до конца (полный игровой цикл
    урона до 0 HP через JS-цикл кастов, см. раздел 7.30 про тайминги): оба клиента синхронно
    получили `match_ended`, победитель увидел золотую "ПОБЕДА", проигравший — красную
    "ПОРАЖЕНИЕ", кнопка возврата корректно очистила экран до спеллбука.
32. **Технический проигрыш при дисконнекте в бою — TODO из `DuelGateway.handleDisconnect`
    снят.** `DuelService.forfeitMatch(matchId, loserId)` — не трогает уже завершённый матч
    (двойной дисконнект/дисконнект после честной победы по HP не переписывает `winnerId`),
    иначе ставит `finishedAt`/`winnerId` ровно как при обычной победе по HP — с точки зрения
    `match_ended` и экрана результата (раздел 7.31) это неотличимо от честной победы, никакого
    отдельного "оппонент отключился" сообщения не показывается. Объявление `match_ended` при
    форфейте — через ОБЩИЙ путь с обычным HP-триггером (`DuelGateway.announceIfFinished`,
    новый private-метод, вынесен из дублировавшегося кода), но добавлена ВТОРАЯ точка вызова —
    в начале каждого тика heartbeat, ДО ветки предматчевого `match_countdown`. Без этого форфейт
    во время 3-секундной паузы после `match_found` завис бы: countdown-ветка тикает через
    `return` раньше, чем код доходил бы до старой (единственной) проверки `finishedAt` в конце
    тика. Грейс-периода на реконнект нет вообще — первый же дисконнект необратимо форфейтит матч
    (см. раздел 8, новый пункт про грейс-период). Проверено в браузере: закрытие вкладки одного
    игрока в активном бою почти мгновенно (в пределах одного heartbeat-тика, ~300мс) показало
    оставшемуся игроку "ПОБЕДА", лог бэкенда подтвердил `Player ... disconnected from match ...`.
33. **Иконки активных баффов/дебаффов над персонажем (раздел 8, было в ожидании).**
    Реализовано ровно по решению из раздела 7.25 — одна иконка на `sourceSpellId` (не общая
    на `EffectType`), картинка переиспользуется из `public/spell-icons/` (той же, что в панели
    каста и спеллбуке). `player_sync.effects[]` уже нёс `sourceSpellId`/`remainingMs`, но не
    было стабильного ключа для `@for track` — раньше при двух эффектах с одинаковым
    `sourceSpellId` (гипотетически) их было бы нечем различить в DOM; добавлено поле `id` на
    `ActiveEffect` (пробрасывается 1:1 в payload). Новый `.effect-icons` в
    `.character-sprite-wrap` (уже `position: relative` из-за летающих циферок, раздел 7.30) —
    маленькая пиксельная иконка с бейджем-таймером `formatCooldown(remainingMs)` в правом
    нижнем углу, `title` = имя заклинания-источника (`SPELL_BY_ID` — новый плоский индекс
    id→SpellButtonData по всем школам в `spells-data.ts`). Проверено в браузере на реальном
    матче: Blaze (DoT на сопернике) и Ember Ward (сам-бафф) оба корректно показали иконку с
    правильной картинкой/подписью и тикающим таймером, эффект и иконка синхронно исчезли по
    истечении длительности.
34. **Баг: спрайт соперника не соответствовал реально выбранной им школе.** Причина —
    `foeMageType` во фронтенде был чисто локальным сигналом (`signal<MageType>('ice')`),
    менявшимся только временной debug-кнопкой (`App.setMageType('foe', ...)`); сервер вообще
    не сообщал клиенту реальную школу соперника. Исправлено на бэкенде: `MatchmakingGateway`
    добавляет `opponentSchool: match.players[opponentId].school` в `match_found` (школа уже
    хранилась в `PlayerState.school`, просто не долетала до клиента). На фронтенде
    `foeMageType` стал `computed` от `socket.match()?.opponentSchool` вместо независимого
    сигнала; TEMP debug-панель ручного выбора школы соперника (`setMageType`, `MAGE_TYPES`,
    `mageTypes`) удалена как источник путаницы — реальные данные всегда точны. Проверено в
    браузере: матч огонь vs хаос, `foeMageType()` на клиенте огня корректно вернул `"chaos"`.
35. **Полоска, отображающая окно каста, поверх букв триггера.** `cast_started` и раньше нёс
    `deadline`, но клиент не показывал, сколько времени реально остаётся до сгорания каста —
    только сами буквы триггера. Разовое (не тикающее) значение `windowMs` добавлено в
    `ActiveCastInfo`: считается как `deadline - Date.now()` РОВНО в момент получения
    `cast_started` (не серверное `deadline - startedAt`), чтобы не завышать полоску на сетевую
    задержку между стартом каста на сервере и приходом события клиенту — тот же принцип, что уже
    использовался в подстраховочном `setTimeout` того же обработчика. Полоска — CSS-анимация
    (`transform: scaleX`, не `width`, чтобы не гонять layout), `animation-duration` привязан к
    `windowMs` через `[style.animationDuration.ms]`; т.к. элемент создаётся заново при каждом
    новом каста (activeCast всегда проходит через `null` между кастами — раздел 6.4), анимация
    свежая на каждый каст без ручного перезапуска. Проверено прямым чтением
    `getComputedStyle(...).transform` в процессе анимации (не скриншотом — 2-секундное окно
    Basic-тира слишком узкое для раздельных tool-вызовов, см. раздел 7.30 про тайминги; тестировал
    на Ultimate-тире, 4 сек): `scaleX` монотонно убывал с реальным течением времени (0.897 на
    401мс → 0.393 на 2419мс от начала 4000мс окна — совпадает с линейной формулой), полоска и
    всё окно корректно исчезали сразу по `spell_resolved`.
36. **Убрана TEMP debug-панель ручного вызова анимации атаки** (кнопки «Атака: вы»/«Атака:
    соперник», `.debug-panel`/`.debug-btn` в CSS) — с раздела 7.29 анимация атаки и так
    проигрывается автоматически на любой `spell_resolved` (см. `effect()` в конструкторе `App`),
    ручной вызов был чистым дублированием, оставленным из более раннего этапа до того, как
    реальные кастовые события были подключены.
37. **Рефакторинг фронтенда: `App` разделён на `Lobby` и `Match`.** До этого весь код (спеллбук,
    боевая сцена, каст, результат матча) жил в одном компоненте `App` — раздел переключался чисто
    через `@if`-блоки внутри одного шаблона. Теперь `App` — тонкий шелл: заголовок + `@if
    (socket.match()) { <app-match /> } @else { <app-lobby /> }`. `Lobby` (`app/lobby/`) владеет
    выбором заклинания для предпросмотра (`selectedSpellId`) и кнопкой «Найти бой»; `Match`
    (`app/match/`) — всей боевой сценой, кастом, кулдаунами, летающими циферками, иконками
    эффектов и экраном результата. Единственное состояние, которое обязано пережить переход
    Lobby → Match — выбранная игроком школа (`youMageType`), т.к. сервер не возвращает игроку его
    же школу обратно (только школу соперника, раздел 7.34) — поэтому оно осталось в `App` и
    прокидывается в оба экрана через `input()`/`output()` (Angular 20 signal-based inputs), а не
    задублировано или поднято в `SocketService`. Побочный эффект разделения: `Match`
    монтируется/размонтируется вместе с самим матчем (`@if` уничтожает и создаёт компонент
    заново), поэтому ручной сброс `youAttacking`/`foeAttacking`/`youFloatingNumbers` и т.д. в
    `returnToMenu()` стал не нужен — новый `Match` создаётся с чистого листа сам; то же для
    `Lobby.selectedSpellId`. `onKeydown`-обработчик клавиш переехал в `Match` и потерял проверку
    "матч вообще есть" — она была нужна только пока обработчик жил в вечно смонтированном `App`.
    CSS разнесён по компонентам (scoped), кроме: `.character-sprite` + анимация idle + скины школ
    — вынесены в общий partial `character-sprite.css` (нужен и `Lobby`-превью, и `Match`-сцене,
    импортируется через `styleUrls` в обоих, чтобы не дублировать пути к спрайтам в двух местах);
    `.keycap-btn` — общая кнопка (нужна и «Найти бой», и «Вернуться на главную») — вынесена в
    глобальный `styles.css`; `prefers-reduced-motion` — тоже в `styles.css`, т.к. Angular
    scoped-стили одного компонента не подействовали бы на разметку другого. Проверено в браузере
    на реальном матче между двумя вкладками: выбор школы в `Lobby` (клик по иконке школы —
    `schoolSelected` доходит до `App`, `App` прокидывает новое значение обратно в оба `input()`),
    переход в `Match` при `match_found`, каст с реальным уроном, победа по дисконнекту соперника,
    экран результата и возврат в чистое лобби — всё отработало корректно.
38. **Баг после раздела 7.37: анимация атаки зацикливалась после первого же успешного каста.**
    Причина — порядок `styleUrls: ['./match.css', '../character-sprite.css']`: Angular
    склеивает CSS component'а в том порядке, что указан в массиве, и в получившемся файле
    `.character-sprite` (из shared partial, `animation: sprite-idle 1.6s steps(1) infinite`)
    оказался ПОСЛЕ `.character-sprite--attack` (в match.css, переопределяет
    `animation-iteration-count: 1`). У обоих селекторов одинаковая специфичность (0,1,0) — при
    равной специфичности выигрывает тот, что идёт позже в каскаде, а не который "логически
    базовый". До разбиения на компоненты (раздел 7.37) оба правила жили в одном `app.css` в
    правильном порядке (база раньше override), поэтому бага не было. Исправлено — порядок
    `styleUrls` изменён на `['../character-sprite.css', './match.css']` (позже вынесено в
    `character.css`, см. ниже). Диагностировано не скриншотами (вкладки автоматизации в этой
    сессии оказались фактически `document.hidden`, Chrome не рендерит для них анимацию вообще —
    визуальные скриншоты показывали статичную идле-позу независимо от бага), а добавлением класса
    `character-sprite--attack` напрямую на реальный (уже отрендеренный Angular'ом, со
    scope-атрибутом) DOM-узел и чтением `getComputedStyle(...).animationIterationCount` — только
    так можно было увидеть, что реально победило в каскаде, независимо от рендеринга/троттлинга.
39. **Иконки баффов/дебаффов перенесены с "над головой" на "над HP-баром, прижаты к левому
    краю"** (по просьбе, раздел 7.33 описывал прежнее место). Переехали из
    `.character-sprite-wrap` (был `position: absolute; top: -1.6rem` по центру) в
    `.character-status`, между именем и HP-баром, в обычном потоке (`display: flex;
    justify-content: flex-start`), с `min-height: 1.8rem`, чтобы HP-бар не прыгал по высоте при
    появлении/исчезновении эффектов. Проверено через `getBoundingClientRect()`: `left` иконки
    точно совпадает с `left` HP-бара, `bottom` иконки примыкает к `top` HP-бара.
40. **Персонаж вынесен в отдельный компонент `Character`** (`app/character/`) — раньше разметка
    бойца (имя, иконки эффектов, HP-бар, летающие циферки, спрайт) дублировалась дважды прямо в
    `match.html` (для "you" и "foe"). Теперь `<app-character>` используется дважды с разными
    input'ами (`side`, `name`, `mageType`, `hpPercent`, `effects`, `floatingNumbers`,
    `attacking`) и одним `output` (`attackAnimationEnd`) — вся боевая СОСТОЯНИЕ (HP, эффекты,
    кулдауны, летающие циферки) по-прежнему в `Match`, `Character` чисто презентационный.
    Технические детали: классы `character`/`character--foe` навешаны на сам host-элемент через
    `host: { class: 'character', '[class.character--foe]': ... }` (а не через обёрточный div),
    поэтому `<app-character>` остаётся прямым потомком CSS Grid `.scene` без лишней вложенности —
    но стилизовать эти host-классы изнутри компонента нужно через `:host`/`:host(.foo)`, а не
    обычные классовые селекторы (Angular не добавляет ngcontent-атрибут на host-элемент, только
    :host его матчит). `Fighter` (`'you'|'foe'`) и `FloatingNumber` вынесены в свои файлы
    (`fighter.ts`, `floating-number.ts`) по образцу `mage-type.ts` (раздел 7.37) — нужны и
    `Match`, и `Character`. `formatCooldown` вынесен в `format.ts` (нужен в обоих компонентах для
    таймера), `effectSpellName` переехал целиком в `Character` (нужен только там). Порядок
    `styleUrls` в `character.ts` — `['../character-sprite.css', './character.css']` — важно
    сохранять именно таким по причине, разобранной в разделе 7.38.
41. **Баг после раздела 7.37: боевая сцена была прижата к низу экрана, а баннер матча
    ("БОЙ НАЧАЛСЯ"/MATCH id) — смещён влево.** Причина та же по духу, что и в разделе 7.38, но на
    уровне разметки, а не CSS-каскада: `<app-lobby>`/`<app-match>` — настоящие DOM-элементы
    (в отличие от старого монолитного `App`, где всё было прямо в одном шаблоне), и по умолчанию
    `display: block`. Это ломало ДВЕ вещи разом: (1) `.content` (баннер матча) внутри `<app-match>`
    терял центрирование — `width: min(100%, 30rem)` резолвился не от ширины страницы, а от ширины
    самого `<app-match>`, который сам ужимался под контент (shrink-to-fit) и центрировался как
    единый блок через `align-items: center` у `.stage`, а `.content` внутри него, не имея
    `margin: 0 auto`, просто прилипал к левому краю ЭТОГО (уже смещённого) блока; (2) `.content`'s
    `flex: 1` переставал работать вообще, т.к. его непосредственный родитель (`<app-match>`)
    — не `display: flex`, а `flex: 1` требует flex-контекста от родителя, иначе не даёт эффекта.
    Из-за (2) всё "лишнее" вертикальное пространство `.stage`-а (`justify-content: space-between`)
    доставалось `.content` (единственному элементу, у которого `flex` теоретически должен был
    расти), в результате `.scene` не получала никакой доли пространства и жалась к низу.
    Исправлено — `app-lobby, app-match { display: contents; }` в `app.css`: убирает box самого
    host-элемента из раскладки, его дети становятся прямыми flex-детьми `.stage`, как будто
    обёртки не было вовсе (ровно как до раздела 7.37). Дальше уже отдельно тюнились: (а)
    вертикальное позиционирование `.scene` — сначала пробовал `margin: auto 0` (симметричное
    авто-центрирование), но выяснилось, что оно "съедает" любую попытку добавить `.scene` высоты
    (например, ради зазора перед панелью заклинаний) — прирост поровну режется на margin-top и
    margin-bottom, нижний край почти не двигается; заменено на явные `margin-top`/`margin-bottom`
    (первый меньше — персонажи не прижаты к баннеру, второй больше — под пунктирной линией
    остаётся честное место); (б) `.cast-window`/`.cast-rejected` (`position: fixed`, к сцене
    геометрически никак не привязаны) подняты отдельно, чтобы окно каста не наезжало ни на
    пунктирную линию сверху, ни на панель заклинаний снизу — итоговые зазоры выровнены до ровно
    20px с обеих сторон. Проверено `getBoundingClientRect()` на живом матче: баннер по центру
    страницы, персонажи в верхней половине экрана, окно каста не пересекается ни с линией, ни с
    панелью заклинаний.

## 8. Открытые вопросы (отложено)

1. ~~UI-группировка одинаковых по смыслу эффектов от разных источников~~ — решено (раздел 7.25,
   реализовано в разделе 7.33): отдельные иконки по `sourceSpellId`, без объединения в "×N".
   Раскладка при большом числе одновременных эффектов (перенос строки и т.п.) пока не проверялась
   вживую — текущая вёрстка просто растягивает `flex`-ряд, не оборачивает.
2. Визуальные ассеты (иконки заклинаний, спрайты персонажей) — вопрос ресурсов
   (художник / генератор изображений с img2img / готовый ассет-пак), не архитектуры.
   Вернёмся при переходе к боевой сцене (PixiJS).
3. Синергии школ / комбо-заклинания (Лёд + Хаос и т.п., см. решение в разделе 7.4) — фаза 2,
   после того как базовая боевая петля проверена в игре.
4. Привязка выбора школы мага к серверному ограничению доступных `spellId` в
   `DuelService.startCast`. Школа хранится на сервере (`PlayerState.school`, раздел 7.12,
   прокинута через matchmaking) и честно отправляется фронтендом в `find_match` (раздел 7.26),
   а с раздела 7.34 сервер ещё и честно сообщает школу соперника обратно клиенту — но
   `startCast` по-прежнему не проверяет, что кастуемый `spellId` принадлежит школе игрока:
   технически можно кастовать чужую.
5. Грейс-период на реконнект (упомянут в разделе 7.32) — сейчас любой дисконнект в бою, даже
   на долю секунды (обновление страницы, кратковременный обрыв сети), необратимо форфейтит
   матч через `DuelService.forfeitMatch`. Не рассматривали, стоит ли давать игроку несколько
   секунд на переподключение (`DuelGateway.handleJoin` уже умеет привязать сокет к
   существующему матчу заново, инфраструктура для реконнекта частично есть) до того, как
   засчитывать поражение.

## 9. Дальнейшие шаги

- ~~JSON/TypeScript-схема данных заклинания для бэкенда~~ — готово (`backend/src/spells/`)
- ~~Реализация `EffectsService` с тикером на NestJS~~ — готово (`backend/src/effects/`)
- ~~Базовый Angular-клиент: лобби, поиск матча, каркас боя~~ — готово (`frontend/`)
- ~~Боевая сцена: обработка `key_input` на клиенте, отображение прогресса каста, HP~~ — готово
  (разделы 7.26/7.30)
- ~~Иконки по школам магии и заклинаниям~~ — готово, растровые (`public/school-icons/`,
  `public/spell-icons/`), не SVG
- Реализация проверки `isTrusted` + расчёт дисперсии таймингов на `DuelGateway`
- ~~Иконки активных эффектов (баффов/дебаффов) над персонажем в бою~~ — готово (раздел 7.33)
